# keycloak-feathers-emitter

> A Keycloak SPI that publishes events to a Featherjs service

## Build the SPI

```shell
mvn clean package
```

## Run the stack

```shell
docker compose up -d
```

## Stop the stack

```shell
docker compose down
```